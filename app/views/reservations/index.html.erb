<h1>Reserved Users For <%= @event.title %></h1>

<% if @event.attending_users.any? %>
    <ul>
        <% @event.attending_users.each do |user| %>
            <li><%= user.name %></li>
        <% end %>
    </ul>

<% else %>
    <h2>No one has reserved to this event yet.</h2>
<% end %>