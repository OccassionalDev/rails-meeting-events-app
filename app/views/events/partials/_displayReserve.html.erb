<% if reservation.nil? %>
    <%= form_tag(event_path) do %>
        <%= submit_tag "RSVP" %>
    <% end %>

<% else %>
    <%= form_tag reservation_path(reservation.id), method: "delete" do %>
        <%= submit_tag "Cancel Reservation" %>
    <% end %>
<% end %>