<% if @reservation.nil? %>
    <%= form_tag(event_reservations_path) %>
        <%= submit_tag "RSVP" %>
    <% end %>

<% else %>
    <%= link_to 'Cancel Reservation', event_reservation_path, method: 'delete' %>
<% end %>