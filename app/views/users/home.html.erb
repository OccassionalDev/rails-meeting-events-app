<h1>All Events</h1>

<div class="wrapper">
    <% @events.each do |event| %>
        <div class='event-card'>
            <%= render partial: 'events/partials/eventCard', locals: { event: event } %>
        </div>
    <% end %>
</div>